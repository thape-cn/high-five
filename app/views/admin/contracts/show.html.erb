<% content_for :head do %>
  <%= append_javascript_pack_tag 'stimulus' %>
  <%= append_javascript_pack_tag 'cable' %>
<% end %>

<%- content_for :action_bar do %>
  <ol>
    <li class="action_bar-item">
      <%= link_to basic_api_contract_path(id: @contract_basic.bpm_id, format: :json), class: "btn btn-outline-primary" do %>
        <%= t("form.basic_api") %> <%= svg_icon('free.svg#cil-plus', %w[icon]) -%>
      <% end %>
    </li>
    <li class="action_bar-item">
      <%= link_to review_api_contract_path(id: @contract_basic.bpm_id, format: :json), class: "btn btn-outline-primary" do %>
        <%= t("form.review_api") %> <%= svg_icon('free.svg#cil-plus', %w[icon]) -%>
      <% end %>
    </li>
  </ol>
<% end %>

<div class="row justify-content-center" data-controller="popover">
  <div class="col-lg-12">
    <div class="container">
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong><%= t('contracts.contract_type') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.contract_type) %>
              <span id="<%= dom_id(@contract_basic, :contract_type) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :contract_type, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.contract_nature') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.contract_nature) %>
              <span id="<%= dom_id(@contract_basic, :contract_nature) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :contract_nature, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong>主合同编号</strong></td>
            <td></td>
            <td><strong>主合同名称</strong></td>
            <td></td>
          </tr>
          <tr>
            <td><strong>来源商机编号</strong></td>
            <td></td>
            <td><strong>来源商机名称</strong></td>
            <td></td>
            <td><strong>工程编号</strong></td>
            <td></td>
            <td><strong>工程名称</strong></td>
            <td></td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.east_boundary') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.east_boundary) %>
              <span id="<%= dom_id(@contract_basic, :east_boundary) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :east_boundary, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.south_boundary') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.south_boundary) %>
              <span id="<%= dom_id(@contract_basic, :south_boundary) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :south_boundary, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.west_boundary') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.west_boundary) %>
              <span id="<%= dom_id(@contract_basic, :west_boundary) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :west_boundary, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.north_boundary') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.north_boundary) %>
              <span id="<%= dom_id(@contract_basic, :north_boundary) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :north_boundary, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong>合同编号</strong></td>
            <td></td>
            <td><strong><%= t('contracts.contract_name') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.contract_name) %>
              <span id="<%= dom_id(@contract_basic, :contract_name) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :contract_name, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.project_land_use_nature') -%></strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.project_land_use_nature) %>
              <span id="<%= dom_id(@contract_basic, :project_land_use_nature) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :project_land_use_nature, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong>线索提供人</strong></td>
            <td></td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.currency') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.currency) %>
              <span id="<%= dom_id(@contract_basic, :currency) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :currency, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.contract_amount') -%>:</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.contract_amount) %>
              <span id="<%= dom_id(@contract_basic, :contract_amount) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :contract_amount, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.split_contract_amount') -%>:</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.split_contract_amount) %>
              <span id="<%= dom_id(@contract_basic, :split_contract_amount) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :split_contract_amount, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.actual_contract_amount') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.actual_contract_amount) %>
              <span id="<%= dom_id(@contract_basic, :actual_contract_amount) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :actual_contract_amount, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.party_a') -%>：</strong></td>
            <td colspan="3">
              <% think, value = split_think_value(@contract_basic.party_a) %>
              <span id="<%= dom_id(@contract_basic, :party_a) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :party_a, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.party_b') -%>：</strong></td>
            <td colspan="3">
              <% think, value = split_think_value(@contract_basic.party_b) %>
              <span id="<%= dom_id(@contract_basic, :party_b) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :party_b, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong>丙方：</strong></td>
            <td></td>
            <td><strong><%= t('contracts.developer_name') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.developer_name) %>
              <span id="<%= dom_id(@contract_basic, :developer_name) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :developer_name, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.scale_type') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.scale_type) %>
              <span id="<%= dom_id(@contract_basic, :scale_type) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :scale_type, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.scale_area') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.scale_area) %>
              <span id="<%= dom_id(@contract_basic, :scale_area) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :scale_area, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong>商务责任公司：</strong></td>
            <td></td>
            <td><strong>商务责任部门：</strong></td>
            <td></td>
            <td><strong>商务负责人：</strong></td>
            <td></td>
            <td><strong>商务协助人：</strong></td>
            <td></td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.beneficiary_bank_account') -%>：</strong></td>
            <td colspan="3">
              <% think, value = split_think_value(@contract_basic.beneficiary_bank_account) %>
              <span id="<%= dom_id(@contract_basic, :beneficiary_bank_account) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :beneficiary_bank_account, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.beneficiary_bank') -%>：</strong></td>
            <td colspan="3">
              <% think, value = split_think_value(@contract_basic.beneficiary_bank) %>
              <span id="<%= dom_id(@contract_basic, :beneficiary_bank) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :beneficiary_bank, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.is_epc') -%>:</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.is_epc) %>
              <span id="<%= dom_id(@contract_basic, :is_epc) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :is_epc, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.is_design_general_contractor') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.is_design_general_contractor) %>
              <span id="<%= dom_id(@contract_basic, :is_design_general_contractor) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :is_design_general_contractor, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong>协议有效期：</strong></td>
            <td></td>
            <td><strong></strong></td>
            <td></td>
          </tr>
          <tr>
            <td><strong>协议适用范围：</strong></td>
            <td></td>
            <td><strong><%= t('contracts.project_tag') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.project_tag) %>
              <span id="<%= dom_id(@contract_basic, :project_tag) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :project_tag, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.quotation_method') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.quotation_method) %>
              <span id="<%= dom_id(@contract_basic, :quotation_method) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :quotation_method, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.construction_and_installation_fee') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.construction_and_installation_fee) %>
              <span id="<%= dom_id(@contract_basic, :construction_and_installation_fee) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :construction_and_installation_fee, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><strong><%= t('contracts.discount_rate') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.discount_rate) %>
              <span id="<%= dom_id(@contract_basic, :discount_rate) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :discount_rate, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td><strong><%= t('contracts.developer_type') -%>：</strong></td>
            <td>
              <% think, value = split_think_value(@contract_basic.developer_type) %>
              <span id="<%= dom_id(@contract_basic, :developer_type) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
                <%= value -%>
              </span>
              <%= link_to invoke_ai_admin_contract_path(id: @contract_basic.id, field_name: :developer_type, category: :basic), data: {controller: "modal", action: "modal#click", 'modal-page-reload-value': true} do -%>
                <%= svg_icon('free.svg#cil-asterisk-circle', %w[icon]) -%>
              <% end %>
            </td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
          </tr>
        </tbody>
      </table>
      <div class="card">
        <div class="card-header"><%= t('contract_reviews.scope_business') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.scope_business) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :scope_business) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.curtain_wall_cert_usage') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.curtain_wall_cert_usage) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :curtain_wall_cert_usage) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.epc_lead_role') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.epc_lead_role) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :epc_lead_role) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-3">
        <div class="card-header"><%= t('contract_reviews.first_payment_prepayment') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.first_payment_prepayment) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :first_payment_prepayment) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.fee_adjustment_downward_only') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.fee_adjustment_downward_only) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :fee_adjustment_downward_only) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.danger_payment_specified') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.danger_payment_specified) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :danger_payment_specified) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.prohibited_split') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.prohibited_split) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :prohibited_split) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.final_payment_rate') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.final_payment_rate) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :final_payment_rate) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-3">
        <div class="card-header"><%= t('contract_reviews.penalty_staff_replacement') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.penalty_staff_replacement) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :penalty_staff_replacement) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.penalty_amount') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.penalty_amount) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :penalty_amount) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.work_suspension_prohibited') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.work_suspension_prohibited) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :work_suspension_prohibited) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-3">
        <div class="card-header"><%= t('contract_reviews.liability_cap_twice_contract') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.liability_cap_twice_contract) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :liability_cap_twice_contract) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.cross_contract_penalty_prohibited') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.cross_contract_penalty_prohibited) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :cross_contract_penalty_prohibited) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
      <div class="card mt-1">
        <div class="card-header"><%= t('contract_reviews.liability_clause_keywords') %></div>
        <div class="card-body">
          <% think, value = split_think_value(@contract_basic.contract_review.liability_clause_keywords) %>
          <p class="card-text">
            <span id="<%= dom_id(@contract_basic, :liability_clause_keywords) -%>" class="think_value" data-coreui-container="body" data-coreui-toggle="popover" data-coreui-placement="top" data-coreui-content="<%= think -%>">
              <%= value -%>
            </span>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
